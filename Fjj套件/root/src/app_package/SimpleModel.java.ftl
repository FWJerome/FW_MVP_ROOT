package ${packageName}.model;
import ${packageName}.contract. ${activityClass}Contract;
import javax.inject.Inject;
import com.fwkj.fw_root_library.BaseModel;

public class ${activityClass}Model extends BaseModel implements ${activityClass}Contract.model {
    public ${activityClass}Model() {
    }
}